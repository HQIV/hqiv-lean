# Accidental copy of `.git/` metadata — never commit
.git_disabled/

.venv/
/.lake
HQIV
hqvmpy
PROtien

# Local doc tooling (Verso etc.); tree is huge — not versioned here.
docbuild/

# Nested Lake build cache for the standalone generators package.
HQIVGenerators/.lake/

# Stray editor/build dumps at repo root (real sources live under Hqiv/).
/Mathlib
/Hqiv.Geometry.AuxiliaryField
/Hqiv.Geometry.OctonionicLightCone

# Archive and scratch — not part of default lake targets (see AGENTS/ASSUMPTIONS.md).
archive/
scratch/
tmp_*.lean

# Extra local venvs (e.g. SAT / tooling experiments)
.venv_sat/

# Root scratch Lean probes (not library modules)
_check_name.lean
_scratch_check.lean

# Agent notes: only ASSUMPTIONS + THEOREMS are versioned
AGENTS/*
!AGENTS/ASSUMPTIONS.md
!AGENTS/THEOREMS.md

# Generated / bench data — keep checked-in witness JSON only
data/*
!data/hqiv_witnesses.json

# scripts: minimal whitelist (build, CI docs, witnesses, Lie-regeneration, Alectryon)
scripts/*
!scripts/build.sh
!scripts/build_api_docs.sh
!scripts/docgen_metadata.py
!scripts/regenerate_hqiv_witnesses.sh
!scripts/export_witnesses.lean
!scripts/alectryon.sh
!scripts/print_lie_bracket_closure.py
!scripts/print_lean_generators.py
!scripts/print_linear_independence.py
!scripts/print_lean_octonion_L.py
scripts/__pycache__/
